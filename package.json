{
    "private": true,
    "workspaces": [
        "benkyo-client",
        "benkyo-server"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix benkyo-server\" \"npm run dev --prefix benkyo-client\""
    },
    "devDependencies": {
        "baseline-browser-mapping": "^2.9.4",
        "concurrently": "^9.2.1"
    }
}
